<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xgf.winecome"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- <service -->
        <!-- android:name="com.baidu.location.f" -->
        <!-- android:enabled="true" -->
        <!-- android:process=":remote" > -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.baidu.location.service_v2.2" > -->
        <!-- </action> -->
        <!-- </intent-filter> -->
        <!-- </service> -->

        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="0W2fh4u8vWGWUuxn0D859o7g" />

        <activity
            android:name="com.xgf.winecome.ui.activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.xgf.winecome.ui.activity.TestActivity" >
        </activity>
        <activity android:name="com.xgf.winecome.ui.activity.SearchActivity" >
        </activity>
        <activity android:name="com.xgf.winecome.ui.activity.GoodsDetailActivity" >
        </activity>

        <service
            android:name="com.xgf.winecome.service.MsgService"
            android:process=":remote" >
            <intent-filter>
                <action android:name="com.xgf.winecome.service.MsgService" />
            </intent-filter>
        </service>
        <service
            android:name="com.xgf.winecome.service.GuardService"
            android:process=":remote" >
            <intent-filter>
                <action android:name="com.xgf.winecome.service.GuardService" />
            </intent-filter>
        </service>
    </application>

</manifest>